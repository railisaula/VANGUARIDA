@model IEnumerable<WebService.Models.Resultados>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Intento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pruebas.Titulo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Respuestas.IdF_Usuario)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Intento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Pruebas.Titulo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Respuestas.IdF_Usuario)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.IdT_Resultado }) |
                @Html.ActionLink("Details", "Details", new { id=item.IdT_Resultado }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.IdT_Resultado })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
